<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <activity android:name=".view.calendar.schedule.ScheduleAddActivity"
            android:exported="true"/>

        <activity android:name=".view.calendar.schedule.ScheduleShowActivity"
            android:exported="true"/>

        <activity android:name=".view.login.LoginActivity"
            android:exported="true"/>

        <activity android:name=".view.main.MainActivity"
            android:exported="true"/>

        <activity android:name=".view.onboarding.OnboardingActivity"
            android:exported="true"/>

        <activity android:name=".view.permission.PermissionActivity"
            android:exported="true"/>

        <activity android:name=".view.profile.ProfileActivity"
            android:exported="true"/>

        <activity android:name=".view.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.tutorial.TutorialActivity"
            android:exported="true"/>

        <activity android:name=".view.webview.WebViewActivity"
            android:exported="true"/>

    </application>
</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wgpgkt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WGPGkt">

        <activity android:name=".views.main.MainActivity" android:exported="true"></activity>
        <activity android:name=".views.main.find.MainFindActivity" android:exported="true"></activity>
        <activity android:name=".views.main.join.MainJoinActivity" android:exported="true"></activity>
        <activity android:name=".views.main.join.general.MainJoinGeneralActivity" android:exported="true"></activity>


        <activity android:name=".views.main.prepare.MainPrepareActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>